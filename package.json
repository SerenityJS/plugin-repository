{
  "private": true,
  "name": "plugin-repository",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build:client": "cd client && bun run build",
    "dev:client": "cd client && bun run dev"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}